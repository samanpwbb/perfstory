node_modules/

# perftale's per-trace index/summary cache
.perftale/

# OS / editor cruft
.DS_Store
*.log
coverage/

# local-only dev traces (clip them into test/fixtures/ with scripts/make-fixture.ts)
temp/